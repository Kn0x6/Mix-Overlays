<!--
    OverlayWindow.xaml — Overlay in-game MixOverlays
    ▸ Affiche chaque matchup verticalement : allié au-dessus de son opposant.
    ▸ Transparent, toujours au-dessus, déplaçable par drag.
-->
<Window x:Class="MixOverlays.Views.OverlayWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:views="clr-namespace:MixOverlays.Views"
        Title="MixOverlays Overlay"
        Background="Transparent"
        AllowsTransparency="True"
        WindowStyle="None"
        Topmost="True"
        ResizeMode="NoResize"
        SizeToContent="WidthAndHeight"
        ShowInTaskbar="False"
        Focusable="False"
        KeyDown="Overlay_KeyDown">

    <Border x:Name="OverlayRoot"
            Background="{StaticResource OverlayBgBrush}"
            BorderBrush="{StaticResource BorderBrush}"
            BorderThickness="1"
            CornerRadius="16"
            Padding="16,12"
            MouseLeftButtonDown="Overlay_MouseLeftButtonDown">
        <Border.LayoutTransform>
            <ScaleTransform x:Name="OverlayScale" ScaleX="1" ScaleY="1"/>
        </Border.LayoutTransform>

        <StackPanel>
            <Grid Margin="0,0,0,8">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                    <Ellipse Width="8" Height="8" Fill="#4DC7DB" Margin="0,0,6,0" VerticalAlignment="Center"/>
                    <TextBlock Text="MixOverlays" Foreground="#80FFFFFF" FontSize="11" FontWeight="SemiBold" VerticalAlignment="Center"/>
                    <TextBlock x:Name="HotkeyHint" Text="  ·  Glisser pour déplacer" Foreground="#70FFFFFF" FontSize="10" VerticalAlignment="Center"/>
                </StackPanel>

                <Button Grid.Column="1"
                        Style="{StaticResource IconButton}"
                        Click="CloseOverlay_Click"
                        ToolTip="Masquer l'overlay"
                        AutomationProperties.Name="Masquer l’overlay">
                    <Viewbox Width="13" Height="13">
                        <Path Data="M 2,2 L 10,10 M 10,2 L 2,10"
                              Stroke="{StaticResource TextSecondaryBrush}"
                              StrokeThickness="1.7"
                              StrokeStartLineCap="Round"
                              StrokeEndLineCap="Round"/>
                    </Viewbox>
                </Button>
            </Grid>

            <Rectangle Height="1" Fill="#20FFFFFF" Margin="0,0,0,10"/>

            <!-- Une colonne = un rôle : l'allié et l'ennemi restent toujours en face. -->
            <ItemsControl x:Name="MatchupList">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <StackPanel Orientation="Horizontal"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Width="162">
                            <TextBlock Text="{Binding Role}"
                                       Height="16"
                                       TextAlignment="Center"
                                       Foreground="#80FFFFFF"
                                       FontSize="9"
                                       FontWeight="Bold"/>
                            <ContentControl Content="{Binding Ally}" Height="236">
                                <ContentControl.ContentTemplate>
                                    <DataTemplate>
                                        <views:DiamondPlayerCard DataContext="{Binding}" Margin="4,0"/>
                                    </DataTemplate>
                                </ContentControl.ContentTemplate>
                            </ContentControl>
                            <Rectangle Height="1" Fill="#20FFFFFF" Margin="4,5"/>
                            <ContentControl Content="{Binding Enemy}" Height="236">
                                <ContentControl.ContentTemplate>
                                    <DataTemplate>
                                        <views:DiamondPlayerCard DataContext="{Binding}" Margin="4,0"/>
                                    </DataTemplate>
                                </ContentControl.ContentTemplate>
                            </ContentControl>
                        </StackPanel>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </StackPanel>
    </Border>
</Window>
